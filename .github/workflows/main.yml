name: Build

on:
    push:
        branches: main
    pull_request:
        branches: main
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu_latest

        steps: 
            - uses: actions/checkout@v4

            - name: Run a code
              run: node index.js

        

